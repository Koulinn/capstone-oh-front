const fadeTopBottomTimer = {
    appear: 0,
    enter: 1300,
    exit: 1300,
};

const fadeTimer = 0;

const chatTimer = 100;

const animationConfig = {
    fadeTopBottomTimer: fadeTopBottomTimer,
    fadeTimer: fadeTimer,
    chatTimer: chatTimer,
};
export default animationConfig;
